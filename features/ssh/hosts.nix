# Central registry of SSH host metadata.
# Used to generate SSH client config on every host.
{
  fuji = {};
  yari = {};
  yuki = {};
  rakku = {};
  aso = {
    port = 8080;
  };
  usu = {
    user = "nix-on-droid";
    port = 2345;
  };
}
